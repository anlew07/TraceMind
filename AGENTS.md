# TraceMind 工程化开发规范

## 项目定位

TraceMind 是一个面向开发者的、本地优先、答案可追溯的 AI 个人知识库系统。

目标：

帮助用户沉淀：

- 技术文档
- 代码资料
- 学习记录
- 历史问题与解决方案

通过 RAG 实现：

- 知识检索
- AI 问答
- 来源追踪
- 经验沉淀

TraceMind 的核心价值：

不是简单的 PDF 聊天工具，而是一个能够记录：

问题 → 分析 → 解决方案 → 代码 → 经验

的个人 AI 工程知识系统。

---

# 当前开发阶段

当前阶段：

v1.1.0 稳定使用、学习、维护与缺陷修复阶段。

优先目标：

- Daily Use：保证导入、检索、问答、知识沉淀和数据恢复闭环可靠。
- Learning：理解现有实现并保留可复查的设计证据。
- Bug Fix：以真实问题和最小修改修复缺陷。
- Retrieval Evaluation：使用固定数据集验证检索变化，不用主观判断替代指标。
- Maintenance：保持依赖、迁移、文档、CI 和恢复能力可维护。

当前不追求：

- 复杂微服务架构
- 过度自动化
- 大规模分布式设计
- 为展示技术而增加复杂组件

---

# 开发原则

## 1. 最小必要修改原则

修改代码时：

优先选择：

- 最小影响范围
- 清晰实现
- 易维护方案

禁止：

- 无明确原因的大规模重构
- 为了优化未来未知场景提前复杂设计
- 修改无关模块

## 2. 问题驱动开发原则

任何重要功能或技术选择，需要回答：

1. 当前解决什么问题？
2. 为什么需要这个功能？
3. 有哪些替代方案？
4. 为什么选择当前方案？
5. 当前方案有什么限制？

不要因为：

"AI推荐" "现在流行"

而直接引入技术。

---

# Truthfulness 原则（真实性）

禁止：

- 编造测试结果
- 编造实验数据
- 声称未运行的测试通过
- 声称未验证的效果提升
- 编造不存在的接口能力

如果没有验证：

必须明确标记：

- TODO
- 待验证
- 假设方案

---

# 学习沉淀模式

TraceMind 不只是代码项目，也是技术学习项目。

对于重要技术决策：

不要只输出：

"这样实现"。

需要解释：

1. 背后的技术原理
2. 为什么选择该方案
3. 其他方案为什么没有采用
4. 未来如何优化

目标：

让我能够理解并解释项目设计。

## 决策记录要求

重要工作必须留下可复查的设计与验证证据：

- 日常功能、缺陷修复和任务过程不再统一追加到单个巨型日志；只在改动需要长期证据时，
  更新对应 Current Doc、ADR、实验记录或 Release Notes。
- 架构、RAG 和技术选型的长期决策记录到 `docs/architecture/adr/` 或对应 Current Design。
- 检索、模型或提示词等效果实验记录到 `docs/experiments/`。

文档事实优先级：

Source Code → Current Documentation → Historical Documentation。

`docs/archive/`、旧 Release Notes 和历史实验不能替代当前代码事实。

记录至少包含：问题、约束、采用方案、未采用方案及原因、遇到的问题、验证方法、结果、遗留风险。

---

# 代码分层与修改规范

保持现有分层边界：

- API 层：处理 HTTP 请求、参数校验、响应和错误映射，不编写复杂业务逻辑。
- Service 层：处理业务规则、用例编排、事务边界和跨资源补偿。
- Repository 层：处理数据库访问，不提交事务、不操作文件。
- Storage 层：处理本地文件写入、校验、移动和删除补偿，不处理 HTTP 或业务规则。
- Parsing 层：处理确定性解析和 Chunking，不访问数据库、HTTP、Celery 或向量服务。
- Integration 层：封装 Redis、Qdrant 和模型服务等外部依赖。

修改前先阅读相关实现、测试、数据模型、迁移和设计文档。

数据库模型变化：

- 必须添加 Alembic migration。
- 必须考虑升级、降级与存量数据兼容性。

全文索引、向量索引或检索结构变化：

- 必须说明存量数据迁移或重建方案。
- 必须保证导入、重解析、更新和删除的索引同步具备幂等性。
- 必须说明失败恢复和回滚方式。

---

# RAG设计要求

涉及 RAG 修改时，需要考虑：

- 为什么需要 RAG
- 文档解析
- Chunk策略
- Metadata设计
- 检索效果验证

模型调用必须通过 Provider 抽象：

- ChatModel、Embedding 和 Reranker 不得在业务代码中绑定单一供应商。
- 新增模型供应商时，应实现 Provider 适配，并记录能力、成本、隐私和降级策略。

回答中的引用：

必须来自真实数据：

- 文件名
- 页码
- 标题
- 文档块
- 章节
- 代码起止行号

禁止：

LLM自行生成不存在的来源。

---

# Agent设计要求

Agent不是默认方案。

引入Agent前，需要回答：

1. 普通流程是否无法解决？
2. Agent增加了什么能力？
3. Agent如何评估效果？
4. Agent失败怎么办？

优先：

确定性流程。

只有需要：

- 多步骤规划
- 工具选择
- 自动决策
- 循环验证

时，引入Agent。

---

# 测试要求

根据修改范围执行测试。

例如：

代码逻辑：

- 单元测试

API修改：

- 接口测试

RAG修改：

需要考虑：

- 检索准确性
- 引用正确性
- 回答质量

不要：

为了通过测试删除断言。

最低检查要求：

- 后端：`uv run ruff check .`、`uv run ruff format --check .`、`uv run mypy app`、相关 `pytest` 测试。
- 前端：`npx vue-tsc --noEmit`、`npx eslint src/ --max-warnings 100`、`npx vitest run`、`npx vite build`。
- 涉及 migration、文件存储补偿、异步任务、索引同步或外部服务时，补充集成测试；若无法自动化，记录原因和替代验证证据。
- RAG 修改需记录固定评测集上的召回质量、引用正确性、回答质量、延迟和成本，并与基线对比。

---

# 数据和安全要求

禁止提交：

- API Key
- 密码
- 私人数据
- 未公开资料

所有配置：

使用环境变量。

涉及远程模型：

需要明确：

- 哪些数据发送出去
- 是否需要脱敏
- 用户是否可控制

---

# Git规范

分支使用：

- `main`：稳定、可发布版本。
- `develop`：日常开发集成。
- `feature/*`：正式功能开发。
- `experiment/*`：技术学习和试验。

禁止直接在 `main` 上开发业务功能。

提交信息：

使用：

- feat:
- fix:
- docs:
- test:
- refactor:
- chore:

---

# 前端设计规范

前端视觉语言以 `docs/design/TraceMind-UI-Design.md` 为单一事实来源。
修改前端表现层前必须先阅读该文档。

核心原则：内容 > UI 装饰。证据 > 装饰。不用表格表示知识对象。不用卡片包裹一切。每页一个主操作。不重复信息。L3 调试默认折叠。

---

# 当前开发优先级

1. 真实日常使用中的数据安全与缺陷修复。
2. 固定评测集上的 Retrieval 质量、引用正确性、回答质量、延迟与成本。
3. 依赖、迁移、CI、文档和跨平台维护。
4. 有明确问题、成功标准和回滚方案的最小体验改进。

不要进行无目标 Feature Expansion。Agent、GraphRAG、Hierarchical Chunking、Auto-merging 或
其他新架构必须先证明普通确定性流程无法解决当前真实问题，并经过单独设计与评测；它们不是
默认下一阶段。

---

# 每次任务结束输出格式

请输出：

## 本次完成

xxx

## 修改文件

xxx

## 设计原因

xxx

## 验证结果

xxx

## 当前问题

xxx

## 下一步建议

xxx
